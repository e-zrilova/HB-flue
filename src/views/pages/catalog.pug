extends ../layouts/default
block content
  section.catalog
    .container.catalog__container
      .catalog__wrapper
        +title-category('Каталог товаров', 'title-category', '')
      .catalog__breadcrumb
        +breadcrumbs(['Главная'], 'Каталог')
      include ../../components/catalog-grid/catalog-grid.pug
block scripts
